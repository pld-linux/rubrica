diff -Nur rubrica-0.9.9.8.orig/Makefile.am rubrica-0.9.9.8/Makefile.am
--- rubrica-0.9.9.8.orig/Makefile.am	Thu Nov 21 10:05:37 2002
+++ rubrica-0.9.9.8/Makefile.am	Tue Nov 26 10:14:30 2002
@@ -17,48 +17,6 @@
 
 EXTRA_DIST = TODO $(rubricadoc_DATA) $(schemas__DATA) rubrica.spec rubrica.desktop
 
-
-install-data-local:
-
-	@$(NORMAL_INSTALL)
-
-	if test -d $(srcdir)/export/stylesheet; then \
-	  $(mkinstalldirs) $(datadir)/rubrica/stylesheet; \
-	  for export in $(srcdir)/export/stylesheet/rub2*; do \
-	    if test -f $$export; then \
-	      $(INSTALL_DATA) $$export $(datadir)/rubrica/stylesheet; \
-	    fi \
-	  done \
-	fi
-
-	if test -d $(srcdir)/export/script; then \
-	  $(mkinstalldirs) $(datadir)/rubrica/script; \
-	  for export in $(srcdir)/export/script/rub2*; do \
-	    if test -f $$export; then \
-	      $(INSTALL_SCRIPT) $$export $(datadir)/rubrica/script; \
-	    fi \
-	  done \
-	fi
-
-	if test -d $(srcdir)/export/nautilus-script; then \
-	  $(mkinstalldirs) $(datadir)/rubrica/nautilus-script; \
-	  for export in $(srcdir)/export/nautilus-script/rubrica*; do \
-	    if test -f $$export; then \
-	      $(INSTALL_SCRIPT) $$export $(datadir)/rubrica/nautilus-script; \
-	    fi \
-	  done \
-	fi
-
-	$(mkinstalldirs) $(datadir)/applications; \
-        if test -f rubrica.desktop; then \
-	        $(INSTALL_DATA) rubrica.desktop $(datadir)/applications; \
-	fi 
-
-	$(mkinstalldirs) $(datadir)/services; \
-	if test -f rubrica.desktop; then \
-		$(INSTALL_DATA) rubrica.desktop $(datadir)/services; \
-	fi
-
 install-schemas: $(schemas__DATA)
 	gconftool-2 --shutdown
 	GCONF_CONFIG_SOURCE=$(GCONF_SCHEMA_CONFIG_SOURCE) gconftool-2 --makefile-install-rule $(schemas_dir)/$(schemas__DATA)
diff -Nur rubrica-0.9.9.8.orig/configure.in rubrica-0.9.9.8/configure.in
--- rubrica-0.9.9.8.orig/configure.in	Sun Nov 24 22:35:05 2002
+++ rubrica-0.9.9.8/configure.in	Tue Nov 26 10:18:44 2002
@@ -186,8 +186,6 @@
 dnl
 dnl ==========================================================================
 
-AM_CONFIG_HEADER(config.h)
-
 AC_OUTPUT([
 Makefile
 rubrica.desktop
